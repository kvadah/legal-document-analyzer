"""AI pipeline package.

Import pipeline modules directly (e.g.
`from app.pipelines.ai.pipeline import run_ai_pipeline`) — deliberately no
eager re-exports here to avoid import cycles with app.llm.
"""
